<UserControl x:Class="SensorFusion.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D30" CornerRadius="5" Padding="15,10" Margin="0,0,0,10">
        </Border>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Foreground" Value="#AAA"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Margin" Value="0,0,5,10"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="{TemplateBinding BorderThickness}" 
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="#0078D4"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TabControl.Resources>

            <TabItem Header="📷 2D Image Stream">
                <GroupBox Header="Raw Stream Buffer" Foreground="White" BorderBrush="#3F3F46" Margin="0,5,0,0">
                    <Grid Background="Black">
                        <Image Source="{Binding DisplayImage}" Stretch="Uniform" RenderOptions.BitmapScalingMode="LowQuality"/>
                    </Grid>
                </GroupBox>
            </TabItem>

            <TabItem Header="🌐 3D Point Cloud (LiDAR)">
                <GroupBox Header="Helix Toolkit 3D Viewport" Foreground="White" BorderBrush="#3F3F46" Margin="0,5,0,0">
                    <Grid Background="#0A0A0A">
                        <TextBlock Text="3D Engine Paused (Ready)" Foreground="#555" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </GroupBox>
            </TabItem>
        </TabControl>

        <GroupBox Grid.Row="2" Header="System Logs" Margin="0,10,0,0" Foreground="White" BorderBrush="#3F3F46">
        </GroupBox>
    </Grid>
</UserControl>